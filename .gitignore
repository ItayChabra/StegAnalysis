# Python stuff
__pycache__/
*.py[cod]
venv/
.env
.pytest_cache/

# PyCharm settings
.idea/

# Datasets (Do NOT upload images to git)
data/
*.jpg
*.png
*.pgm

# ...except the bundled demo-mode sample images the frontend ships with
!frontend/public/demo/
!frontend/public/demo/*.png

# Datasets and Auto-generated JSON
dataset_split.json
*.csv
training/evaluation_results/
srnet_epoch_*.pth
srnet_ft_epoch_*.pth

# Run logs / one-off benchmark dumps — not part of the deliverable
*.log

# Orphaned local scripts/docs kept on disk but not tracked (see cleanup commit)
class_demo.py
kaggle_bench_conclusion.md
smoke_test_canonical.py
